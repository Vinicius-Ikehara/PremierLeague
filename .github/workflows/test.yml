name: Run Colab Notebook

on:
  schedule:
    - cron: '25 11 15 2 *'  # Execute às 11h16 de 15 de fevereiro de 2025

jobs:
  run-colab:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run Colab Notebook
      run: |
        pip install colab-cli
        colab-cli login --username vhikehara@gmail.com --password Feij@O310b1
        colab-cli run --notebook PremierLeagueData.ipynb
